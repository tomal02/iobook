<!-- <% if !user_signed_in? %>
  <% controller.redirect_to "/" %>
<% end %>

<%= render "layouts/navbar" %>


<%= link_to new_post_path do %>
  New post
<% end %>

                    <section id="new_post_container">
									<section id="new_post">
										<%= form_for @post do |form| %>
                      <%= form.text_area :message %>
                      <%= form.submit 'Submit' %>
                    <% end %>
									</section>
								</section>
<% @posts.each do |post| %>
  <p><%= post.name %></p>
  <p><%= simple_format(post.message)   %></p> <br>
  <p><%= post.created_at.localtime.strftime("%H:%M %d/%m/%y") %></p>
<% end %> -->

	<% if !user_signed_in?%>
		<% controller.redirect_to "/" %>
	<% end %>	

	<%= render "layouts/navbar" %>

  <section id="page_container">

    <section id="new_post_container">
		<section id="new_post">
			<%= form_for @post do |form| %>
      <%= form.text_area :message %>
  
      <div id="new_post_button" class="btn:hover"><i class="fas fa-pencil-alt"></i>
      <%= form.submit 'Post' %></div>
  
      <% end %>
		</section>
  </section>
  	
	<% @posts.each do |post| %>
		<section id="post_container">
			<div>
				<p><%= post.name %></p>
				
				<% if current_user.avatar.attached? %>
					<%= image_tag url_for(current_user.avatar), class: %w(post_image_size)%>
				<% end %>
			</div>
			<button onclick="togglePostOptions()" class="blank_btn"><i class="fas fa-ellipsis-h"></i></button>
			<div>
				<p>
				<%= post.name %>
				</p>
				<p>
					<%= post.created_at.localtime.strftime("%H:%M") %>
				</p>
				<p>
					<%= post.created_at.localtime.strftime("%d/%m/%y") %>
				</p>
			</div>
			<p>
					<p><%= simple_format(post.message)   %></p>
			</p>
			<aside id="post_options">
				<%= link_to 'Edit post' , edit_post_path(post.id) %><br>
					<hr class="divider">
					<%= link_to 'Delete post' , post_path(post.id), method: :delete, data: {
						confirm: "Are you sure you want to delete?" } %>
			</aside>
		</section>
	<% end %>
</section>

